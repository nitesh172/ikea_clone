import { TRY } from "./actionTypesHome"

export const reducerHome = (state, action) => {
    switch (action.type) {
        case TRY:
            break;
    
        default:
            return {...state}
    }
}